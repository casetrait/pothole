import TicketHeader from "../TicketHeader/TicketHeader";


export default function TicketItems(props) {
    return (
        <div className="">
            <p>{props.title}</p>
            {/* <TicketHeader />
            <TicketDetails /> */}
        </div>
    );
}